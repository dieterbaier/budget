// The feature's public API — CON-005. See the note in the monthly-expenditure
// barrel: named exports only, extended on demand.
export { RecordTransactionForm } from './components/RecordTransactionForm'
